--- a/auth/oauth2/classes/auth.php
+++ b/auth/oauth2/classes/auth.php
@@ -143,6 +143,21 @@ class auth extends \auth_plugin_base {
      */
     public function can_reset_password() {
-        return false;
+        return true;
     }

+    /**
+     * Store a local password hash so the user can log in with username/password
+     * after completing the password-reset flow. Auth is switched to 'manual'
+     * by local_geoauth_post_set_password_requests() once this succeeds.
+     *
+     * @param object $user
+     * @param string $newpassword plaintext
+     * @return bool
+     */
+    public function user_update_password($user, $newpassword) {
+        return update_internal_user_password($user, $newpassword);
+    }
+
